package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Professional Broiler Green Palette
val GreenPrimary = Color(0xFF1B5E20)        // Deep forest green for branding & key accents
val GreenDarkText = Color(0xFF0D3B16)       // Extra deep green for high-contrast titles on light bg
val GreenButton = Color(0xFF2E7D32)         // Bright, inviting green for primary buttons
val GreenSecondary = Color(0xFF388E3C)      // Medium green
val GreenLight = Color(0xFF4CAF50)          // Vibrant green accent
val GreenContainer = Color(0xFFEBF5EE)      // Soft pastel green container
val GreenContainerBorder = Color(0xFFC7E2CE)// Subtle border for green containers
val OnGreenContainer = Color(0xFF0A3D14)    // Text on green containers

// Field Accent (Amber / Orange)
val OrangeAccent = Color(0xFFE65100)        // Vibrant orange for key indicators
val AmberAccent = Color(0xFFF57F17)         // Warm amber
val YellowAccent = Color(0xFFFFB300)        // Secondary accent
val AmberContainer = Color(0xFFFFF8E7)      // Soft cream container
val AmberBorder = Color(0xFFFFE5A3)         // Soft amber border
val OnAmberContainer = Color(0xFF663D00)    // High contrast text on amber

// Neutral & Backgrounds (Crisp, modern Light Theme)
val PureWhite = Color(0xFFFFFFFF)
val OffWhiteBg = Color(0xFFF8FAF9)          // Soft clean background
val CardSurface = Color(0xFFFFFFFF)         // White card surface
val CardBorder = Color(0xFFE2E9E4)          // Clean crisp subtle border
val DividerColor = Color(0xFFEDF2EE)        // Soft divider
val InputBorder = Color(0xFFB8C5BD)          // Clear, distinct border for input fields
val InputBorderFocused = Color(0xFF2E7D32)   // Vibrant green border on focus

// Typography
val TextDarkPrimary = Color(0xFF181D1A)     // High-contrast primary text
val TextDarkSecondary = Color(0xFF4E5851)   // Readable secondary text
val TextMuted = Color(0xFF758279)           // Subdued helper text

// Status colors
val RedError = Color(0xFFC62828)
val RedErrorContainer = Color(0xFFFFEBEE)
val RedBorder = Color(0xFFFFCDD2)
